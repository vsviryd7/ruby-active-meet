<p>Events for <%= @sport.name %>:</p>
<ul>
  <% @sport.events.each do |event| %>
    <li>
      <%= link_to "#{event.location} (#{event.event_time.strftime("%B %d, %Y %H:%M")})", event_path(event) %>
    </li>
  <% end %>
</ul>
