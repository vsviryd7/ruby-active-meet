<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Active Meet" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- PWA manifest (optional) -->
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Top Nav -->
    <header style="padding:10px 16px; border-bottom:1px solid #eee;">
      <nav style="display:flex; gap:12px; align-items:center; justify-content:space-between; max-width:960px; margin:0 auto;">
        <div style="display:flex; gap:12px; align-items:center;">
          <%= link_to "ActiveMeet", root_path, style: "font-weight:700; text-decoration:none;" %>
          <%= link_to "Events", events_path %>
          <%= link_to "Sports", sports_path %>
          <%= link_to "Users",  users_path %>
        </div>

        <div>
          <% if logged_in? %>
            <span>Hi, <%= current_user.name %> (<%= current_user.role %>)</span>
            <%= button_to "Log out", logout_path, method: :delete, form: { style: "display:inline" } %>
          <% else %>
            <%= link_to "Sign up", signup_path %> |
            <%= link_to "Log in",  login_path %>
          <% end %>
        </div>
      </nav>
    </header>

    <!-- Flash messages -->
    <div style="max-width:960px; margin:16px auto 0;">
      <% if notice %>
        <p style="background:#e8fbef; border:1px solid #b6efc8; padding:8px 12px; border-radius:6px;"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p style="background:#ffefef; border:1px solid #f5b5b5; padding:8px 12px; border-radius:6px;"><%= alert %></p>
      <% end %>
    </div>

    <!-- Page content -->
    <main style="max-width:960px; margin:16px auto; padding:0 8px;">
      <%= yield %>
    </main>
  </body>
</html>