<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Active Meet" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- PWA manifest (optional) -->
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Top Nav -->
    <header>
      <nav>
        <div style="display:flex; gap:12px; align-items:center;">
          <%= link_to "ActiveMeet", root_path, style: "font-weight:700; text-decoration:none;" %>
          <%= link_to "Events", events_path %>
          <%= link_to "Sports", sports_path %>
          <%= link_to "Users",  users_path %>
        </div>

        <div>
          <% if logged_in? %>
            <span>Hi, <%= current_user.name %> (<%= current_user.role %>)</span>
            <%= button_to "Log out", logout_path, method: :delete, class: "btn btn-outline", form: { class: "button_to" } %>
          <% else %>
            <%= link_to "Sign up", signup_path %> |
            <%= link_to "Log in",  login_path %>
          <% end %>
        </div>
      </nav>
    </header>

    <div class="banner">
      <%= image_tag "sports-tools.jpg", alt: "ActiveMeet Banner" %>
    </div>

    <!-- Flash messages -->
    <div class="flash">
      <% if notice %><p class="notice"><%= notice %></p><% end %>
      <% if alert  %><p class="alert"><%= alert  %></p><% end %>
    </div>

    <!-- Page content -->
    <main>
      <%= yield %>
    </main>
  </body>
</html>